#ifndef PLAYER_H
#define PLAYER_H


#include <SDL2/SDL.h>
#include <SDL2/SDL_ttf.h>
#include <string>
class Player {
public:
    Player();
    ~Player();

private:
    // Unit units[4];
    int mana;
    
};

#endif